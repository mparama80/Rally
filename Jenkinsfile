pipeline {
	agent any
		stages {
			stage('First') {
				steps {
				echo "This is declarative pipeline"
				}
					}
			stage('Second') {
				steps {
				echo "This is second stage"
				}
			}
		}
}
